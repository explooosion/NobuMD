import { CharactorSearchPipe } from './charactor-search.pipe';

describe('CharactorSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CharactorSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
